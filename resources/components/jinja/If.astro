---
import Tag from "./Tag.astro";

export interface Props {
    expression: string;
}

const { expression } = Astro.props as Props;
---

<Tag expression={`if ${expression}`} />
<slot />
<Tag expression="endif" />
